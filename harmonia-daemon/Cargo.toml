[package]
name = "harmonia-daemon"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
homepage.workspace = true
repository.workspace = true

[dependencies]
# TODO: Update to use new Nix.rs-based crates in Phase 3
# harmonia-store-core = { path = "../harmonia-store-core" }  # Legacy - now harmonia-store-core-legacy
# harmonia-store-remote = { path = "../harmonia-store-remote" }  # Legacy - now harmonia-store-remote-legacy
tokio = { workspace = true }
log = { workspace = true }
env_logger = { workspace = true }
thiserror = { workspace = true }
toml = { workspace = true }
serde = { workspace = true }
rusqlite = { version = "0.37", features = ["bundled"] }

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
